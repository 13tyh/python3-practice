"""アルゴリズムと計算量の基礎練習。"""


def linear_search(values: list[str], target: str) -> int:
    """targetのindexを返す。なければ-1。"""
    # TODO
    raise NotImplementedError


def binary_search(sorted_values: list[int], target: int) -> int:
    """sort済みlistからtargetのindexを二分探索で返す。"""
    # TODO
    raise NotImplementedError


def dedupe_keep_order(values: list[str]) -> list[str]:
    """順序を保って重複を除く。"""
    # TODO
    raise NotImplementedError


def top_n_by_score(scores: dict[str, int], n: int) -> list[str]:
    """score降順、同点はname昇順で上位n件のnameを返す。"""
    # TODO
    raise NotImplementedError
